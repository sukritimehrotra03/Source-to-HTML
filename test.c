/* this is multi-line comment */
#include <stdio.h>
#include "header.h"

int main()
{
    int x = 10;
    // This is a for loop
    for(int i = 0; i < 15; i++)
    {
        printf("X is %d\n", x);
    }
    return 0;
}